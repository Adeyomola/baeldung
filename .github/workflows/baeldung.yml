name: cd Command Demo
on:
  push:
    branches:
      - main
jobs:
  demo:
    name: demo
    runs-on: ubuntu-latest
    steps:
      - run: |
          cd directory_1
          cat file_1
      - run: |
          cd directory_2
          cat file_2
